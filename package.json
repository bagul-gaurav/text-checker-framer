{
  "name": "framer-qc-tool",
  "version": "2.0.0",
  "description": "On-demand web tool: drop a .docx + paste a staging URL, get a report of any doc content missing from the page. Uses a hosted scraping API (no self-hosted browser).",
  "private": true
}
